import { StoreAdapter } from '../store-adapter';

export interface RedisAdapterConfig {}

/**
 * Connects to a Redis-compatible store.
 */
export class RedisAdapter implements StoreAdapter {
  boot() {}
}
